#!/bin/sh
# This script is used to spawn a terminal instances
# with a different title and class, and if I ever change
# my default terminal I only have to change the flags once
#   Usage:-
#   uniqueterm class title/instance

$TERMINAL --title "$2" --class "$2","$1"
